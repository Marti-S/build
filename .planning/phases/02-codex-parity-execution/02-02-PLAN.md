---
phase: 02-codex-parity-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/claude/AGENTS.md
  - get-shit-done/templates/opencode/AGENTS.md
  - .planning/phases/02-codex-parity-execution/02-02-SUMMARY.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Repository includes baseline Claude AGENTS guidance for /gsd (Codex-free)"
    - "Repository includes baseline OpenCode AGENTS guidance for /gsd (Codex-free)"
    - "Baseline templates enable verification that Codex installs do not alter Claude/OpenCode guidance"
  artifacts:
    - path: "get-shit-done/templates/claude/AGENTS.md"
      provides: "Baseline Claude GSD guidance template with GSD marker block"
    - path: "get-shit-done/templates/opencode/AGENTS.md"
      provides: "Baseline OpenCode GSD guidance template with GSD marker block"
  key_links:
    - from: "get-shit-done/templates/claude/AGENTS.md"
      to: "GSD marker block"
      via: "GSD guidance section"
      pattern: "<!-- GSD:START -->"
    - from: "get-shit-done/templates/opencode/AGENTS.md"
      to: "GSD marker block"
      via: "GSD guidance section"
      pattern: "<!-- GSD:START -->"
---

<objective>
Add baseline Claude/OpenCode AGENTS templates so Codex installs can be verified as non-invasive.

Purpose: Provide repo evidence that Codex support leaves existing guidance unchanged.
Output: Claude and OpenCode AGENTS templates with GSD marker blocks and Codex-free guidance.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-parity-execution/02-CONTEXT.md
@.planning/phases/02-codex-parity-execution/02-RESEARCH.md
@.planning/phases/02-codex-parity-execution/02-codex-parity-execution-VERIFICATION.md

@get-shit-done/templates/codex/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create baseline Claude AGENTS template</name>
  <files>get-shit-done/templates/claude/AGENTS.md</files>
  <action>
Create the Claude AGENTS template with the same structure as the Codex template, but without any Codex-only guidance.

- Add directory `get-shit-done/templates/claude/` if missing.
- Write `AGENTS.md` with a short intro, GSD marker block, and a "GSD /gsd guidance (Claude)" header.
- Use the same general bullets as Codex guidance, but exclude Codex-specific items:
  - Do NOT mention Codex, repo-scoped Codex config, or `@~/.claude/get-shit-done` mapping.
  - Do NOT include the explicit `commands/gsd/<command>.md` mapping line.
- Keep the Notes section identical in tone to the Codex template.
  </action>
  <verify>Open `get-shit-done/templates/claude/AGENTS.md` and confirm the GSD marker block exists and contains no Codex-specific bullets.</verify>
  <done>Claude AGENTS template exists with a GSD marker block and Codex-free guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Create baseline OpenCode template and verify Codex-free content</name>
  <files>get-shit-done/templates/opencode/AGENTS.md; get-shit-done/templates/claude/AGENTS.md</files>
  <action>
Create the OpenCode AGENTS template mirroring the Claude template, then confirm both templates include the GSD markers and avoid Codex-only language.

- Add directory `get-shit-done/templates/opencode/` if missing.
- Write `AGENTS.md` with the same structure as the Claude template.
- Use the header "GSD /gsd guidance (OpenCode)" and keep bullets identical to the Claude template.
- Exclude all Codex-specific guidance and mapping lines (same exclusions as Claude).
- Confirm the marker block exists in both files (`<!-- GSD:START -->` and `<!-- GSD:END -->`).
- Ensure neither file includes "Codex" or the `@~/.claude/get-shit-done` path-mapping line.
  </action>
  <verify>Run `rg -n "<!-- GSD:START -->" get-shit-done/templates/claude/AGENTS.md get-shit-done/templates/opencode/AGENTS.md` and `rg -n "Codex|@~/.claude/get-shit-done" get-shit-done/templates/claude/AGENTS.md get-shit-done/templates/opencode/AGENTS.md || true` (expect no matches for the second).</verify>
  <done>OpenCode and Claude templates both include GSD markers and contain no Codex-specific guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Write execution summary</name>
  <files>.planning/phases/02-codex-parity-execution/02-02-SUMMARY.md</files>
  <action>
Create the phase summary using the standard summary template and capture what changed, verification performed, and any follow-ups.

- Use `@~/.config/opencode/get-shit-done/templates/summary.md` as the structure.
- Include references to the new templates and the verification checks.
  </action>
  <verify>Open `.planning/phases/02-codex-parity-execution/02-02-SUMMARY.md` and confirm it follows the summary template with outcomes and verification results.</verify>
  <done>02-02 summary exists with outcomes and verification notes.</done>
</task>

</tasks>

<verification>
- Both baseline templates exist and include GSD marker blocks.
- No Codex-specific guidance appears in the Claude/OpenCode templates.
</verification>

<success_criteria>
- `get-shit-done/templates/claude/AGENTS.md` and `get-shit-done/templates/opencode/AGENTS.md` exist.
- Each template contains a GSD marker block with general /gsd guidance only.
- Verification report can now confirm Claude/OpenCode guidance remains unchanged after Codex support installation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-codex-parity-execution/02-02-SUMMARY.md`
</output>
